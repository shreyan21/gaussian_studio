# Dependencies required by the selected PyTorch/Torchvision wheels on Windows.
# Kept on PyPI; torch/torchvision themselves come from the official CPU/CUDA index.
sympy==1.14.0
networkx==3.6.1
Jinja2==3.1.6
